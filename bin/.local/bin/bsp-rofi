#!/bin/bash

if [ -f "$HOME/.profile" ]; then source "$HOME/.profile" ; else y="25" ;fi
if [ $style == "minimal" ]; then  y="-250" ; else y="25" ;fi

tile=$(bsp-layout layouts | rofi -dmenu -monitor -3 -yoffset $y -width 50% -theme ~/.config/rofi/themes/layouts.rasi)
bsp-layout set $tile

