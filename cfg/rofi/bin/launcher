#!/usr/bin/env bash

[[ -n "$(rofi -h | grep 'calc')" ]] && mode="drun,filebrowser,calc" || mode="drun,filebrowser"

rofi \
	-no-lazy-grab \
	-show drun \
	-modi "$mode" \
	-drun-display-format "{name}" \
	-theme launcher
