// Waybar config file

{
// Configuration for the bottom bar
	
	"name": "main-bar", 			// Waybar name
	"id": "main-bar",	 			// Waybar id
    "layer": "top",					// Waybar at top layer
    "position": "bottom", 			// Waybar position (top|bottom|left|right)
    "exclusive": true, 				// Request an exclusive zone from the compositor
    "passthrough": false, 			// Pass any pointer events to the window under the bar
    "height": 23, 					// Waybar height (to be removed for auto height)
    "width": "", 					// Waybar width (empty for auto width)
    "spacing": 2, 					// Gaps between modules (0px) Adjusted in the css
    "margin": 0, 					// Space around the bar
    "margin-top": 0, 				// Space above the bar
    "margin-bottom": 5, 			// Space below the bar
    "margin-left": 80, 				// Space to the left of the bar
    "margin-right": 80, 			// Space to the right of the bar
    "fixed-center": true, 			// Center modules position
    //"ipc": true, 					// Control waybar with `swaymsg bar` commands

    "include": "~/.config/waybar/modules",

    // Choose the order of the modules
    
    "modules-left": [
        "custom/playpause",
    	"custom/playerctl",
 	    "hyprland/window",	

    ],
    
    "modules-center": [
        "wlr/workspaces",  
    ],
    
    "modules-right": [
		"custom/eww",
        "pulseaudio",
        "backlight",
        "battery",
        "clock",
        "tray", 
        "custom/wall",
    ],
}
